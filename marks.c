// Program to display total marks and average marks
#include<stdio.h>

void main()
{
 int maths,english,total,average;

    maths = 75;
    english = 60;
    total = maths+english;
    average = total/2;
    printf("Total = %d\n",total);
    printf("Average = %d",average);
}
